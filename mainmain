#include <iostream>
#include <cstdlib>
#include "stos.h"

using namespace std;

int main()
{
    int li;

    stos stosik;

    stosik.dodaj(2);
    stosik.dodaj(3);
    stosik.dodaj(4);

    stosik.wypisz();

    cout<<endl;
    cout<<"Podaj liczbe:";
   // stosik.usun();
   cin >> li;
   if(stosik.jest(li))   stosik.usel(li);

   stosik.wypisz();

   //if(stosik.jest(8))
 //       cout<<"Jest";
 //  else
    //    cout<<"Nie ma";
   // stosik.wypisz();


    return 0;
}
