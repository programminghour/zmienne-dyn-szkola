#include <iostream>
#include <cstdlib>

using namespace std;

int main()
{
    int li;
    stos stosik;

    stosik.dodaj(2);
    stosik.dodaj(3);
    stosik.wypisz();

    cout<<endl;


     cout<<"Podaj liczbe:";
   // stosik.usun();
   if(stosik.jest(4))
        cout<<"Jest";
   else
        cout<<"Nie ma";
   // stosik.wypisz();
   
    return 0;
}
